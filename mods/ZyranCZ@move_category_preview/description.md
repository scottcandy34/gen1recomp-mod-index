Replaces the word “TYPE” in the move selection preview with “PHYS” or “SPEC” according to the move’s category.

When the physical/special split is active (or when using a related balance mod) this gives an instant visual cue of how the selected move will be calculated without needing to open a separate info screen.
